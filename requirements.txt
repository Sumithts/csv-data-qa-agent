pandas==2.2.2
numpy==1.26.4
openai==1.51.0
httpx==0.27.2
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2
tenacity==9.0.0
rich==13.8.1
streamlit==1.39.0
plotly==5.24.1
pytest==8.3.3
reportlab==4.4.10

# Optional: enables embedding-based semantic query caching (better paraphrase
# matching than the built-in difflib fallback). Not required — the agent
# runs fine without it, just with a slightly less forgiving cache.
# sentence-transformers==3.0.1

openpyxl==3.1.5
